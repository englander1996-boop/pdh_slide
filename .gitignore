# =====================================================================
#  LaTeX / latexmk build artifacts (regenerated by `latexmk -lualatex main.tex`)
#  main.pdf is the deliverable and IS tracked — do not ignore it.
# =====================================================================
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.vrb
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.run.xml
*.bcf
*.idx
*.ilg
*.ind

# ---- 一時チェックビルド / ページ確認用レンダリング（プロジェクト直下のみ）----
# figures/ 配下の図版 PNG は追跡対象なので、ルート直下の一時 PNG だけを無視する
/chk_*.png
/pgchk-*.png
/preview-*.png
/_chk*
/_diag*.log
/build_*.log

# ---- OS / エディタ ----
Thumbs.db
.DS_Store

# ---- Claude Code （ローカル設定・セッション） ----
.claude/

# 退避フォルダ（不要ファイルの一時置き場、いつでも復元可）
/_bin/

# ---- 隔離ビルド用ドライバ / ページ確認レンダリング（ルート直下のみ）----
/_drv*
/_s*
/_p[0-9]*.png
/dpv*.png
